[package]
name = "forgeyard-protocol"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later OR Commercial"

[dependencies]
serde = { workspace = true, features = ["derive"] }
forgeyard-model = { path = "../forgeyard-model" }
uuid = { workspace = true }
bytes = { workspace = true }
